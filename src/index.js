const React = require("react");
const ReactDOM = require("react-dom");

ReactDOM.render(
<div>
  <h1>Tonight's Todo's...</h1>
  <p>The same thing that we try to do every night. Try to take over the world.</p>
</div>, 
  document.getElementById("root")
);